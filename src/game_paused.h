#ifndef GAME_PAUSED_H
#define GAME_PAUSED_H

#include <stdint.h>

// 'extern' tells the compiler: "This exists somewhere else, don't worry about it yet."
extern const uint8_t game_paused_map[];

#endif